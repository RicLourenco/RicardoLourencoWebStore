@model IEnumerable<RicardoLourencoWebStore.Models.ProductViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastPurchase)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastSale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAvailable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stock)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @if (!string.IsNullOrEmpty(item.ImageUrl))
                {
                    <img src="@Url.Content(item.ImageUrl)" alt="Image" style="width:100px; height:150px; max-width:100%; height:auto" />
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastPurchase)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastSale)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAvailable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stock)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
        }

        
        @*<ejs-grid id="Grid" dataSource="Model" allowPaging="true" allowReordering="true" allowExcelExport="true"
                  allowFiltering="true" allowPdfExport="true" allowSorting="true"
                  toolbar="@(new List<string>() { "Add", "Edit", "Delete", "Cancel", "Update" })">
            <e-data-manager url="/Products/UrlDataSource" insertUrl="/Products/Insert" updateUrl="/Products/Update" removeUrl="/Products/Remove"adaptor="UrlAdaptor"></e-data-manager>
            <e-grid-editsettings showDeleteConfirmDialog="true" allowAdding="true" allowEditing="true" allowDeleting="true"></e-grid-editsettings>
            <e-grid-columns> 
                <e-grid-column field="Name" headerText="Name" validationRules="@new{required=true}"></e-grid-column>
                <e-grid-column field="Price" headerText="Price" validationRules="@new{required=true}"></e-grid-column>
                <e-grid-column field="LastPurchase" headerText="Last purchase" validationRules="@new{required=true}"></e-grid-column>
                <e-grid-column field="LastSale" headerText="Last sale" validationRules="@new{required=true}"></e-grid-column>
                <e-grid-column field="IsAvailable" headerText="Is product available?" validationRules="@new{required=true}"></e-grid-column>
                <e-grid-column field="Stock" headerText="Stock" validationRules="@new{required=true}"></e-grid-column>
            </e-grid-columns>
        </ejs-grid>*@

    </tbody>
</table>